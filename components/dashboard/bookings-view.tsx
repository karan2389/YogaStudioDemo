"use client";

import { useEffect, useState } from "react";
import Link from "next/link";
import { CalendarDays, Clock3, RotateCcw } from "lucide-react";
import { DashboardPageHeader } from "@/components/dashboard/dashboard-page-header";
import { Button } from "@/components/ui/button";
import { AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle, AlertDialogTrigger } from "@/components/ui/alert-dialog";
import { useDemoSession } from "@/hooks/use-demo-session";
import { canCancelBooking, cancelDemoBooking, getDemoBookings } from "@/services/demo-storage";
import type { DemoBookingRecord } from "@/types/demo";

export function BookingsView() {
  const { session } = useDemoSession();
  const [items, setItems] = useState<DemoBookingRecord[]>([]);
  useEffect(() => { const load = () => { if (session) setItems(getDemoBookings().filter((item) => item.customerId === session.id)); }; load(); window.addEventListener("ananda-demo-change", load); return () => window.removeEventListener("ananda-demo-change", load); }, [session]);
  return <><DashboardPageHeader eyebrow="My bookings" title="Your sessions." copy="View confirmed and cancelled bookings. Cancellations are available until two hours before class." action={<Button asChild className="h-11 rounded-full bg-[#254d3f] text-white"><Link href="/schedule">Book another class</Link></Button>} />
    <div className="grid gap-4">{items.length ? items.map((booking) => { const date = new Date(booking.startsAt); const cancellable = canCancelBooking(booking); return <article key={booking.id} className="rounded-[1.5rem] border border-[#17362d]/10 bg-white p-5 shadow-[0_8px_28px_rgba(36,57,47,0.04)] sm:p-6"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-center"><div className="flex gap-4"><span className={`grid size-12 shrink-0 place-items-center rounded-xl ${booking.status === "confirmed" ? "bg-[#e3ece5] text-[#315744]" : "bg-[#eeeae5] text-[#7a746d]"}`}><CalendarDays className="size-5" /></span><div><div className="flex flex-wrap items-center gap-2"><h2 className="font-display text-2xl">{booking.className}</h2><span className={`rounded-full px-2.5 py-1 text-[0.68rem] font-bold uppercase ${booking.status === "confirmed" ? "bg-[#e3ece5] text-[#315744]" : "bg-[#eeeae5] text-[#6f6963]"}`}>{booking.status}</span></div><p className="mt-1 text-sm text-[#65756e]">{date.toLocaleDateString("en-IN", { weekday: "long", day: "numeric", month: "long", timeZone: "Asia/Kolkata" })} · {date.toLocaleTimeString("en-IN", { hour: "numeric", minute: "2-digit", timeZone: "Asia/Kolkata" })}</p><p className="mt-1 text-xs text-[#809087]">{booking.instructorName} · {booking.bookingType === "trial" ? "Trial session" : "Single session"} · {booking.id}</p></div></div><div className="sm:text-right"><p className="font-semibold">₹{booking.amount}</p>{booking.status === "cancelled" ? <p className="mt-1 text-xs font-semibold text-[#315744]"><RotateCcw className="mr-1 inline size-3" />Refunded</p> : cancellable ? <AlertDialog><AlertDialogTrigger asChild><Button variant="outline" size="sm" className="mt-2 rounded-full bg-transparent text-[#8b3d32]">Cancel booking</Button></AlertDialogTrigger><AlertDialogContent className="rounded-[1.5rem]"><AlertDialogHeader><AlertDialogTitle>Cancel {booking.className}?</AlertDialogTitle><AlertDialogDescription>This booking is outside the two-hour cutoff. The demo payment of ₹{booking.amount} will be marked as refunded.</AlertDialogDescription></AlertDialogHeader><AlertDialogFooter><AlertDialogCancel className="rounded-full">Keep booking</AlertDialogCancel><AlertDialogAction className="rounded-full bg-[#9a4337] text-white hover:bg-[#7f342b]" onClick={() => cancelDemoBooking(booking.id)}>Cancel and refund</AlertDialogAction></AlertDialogFooter></AlertDialogContent></AlertDialog> : <p className="mt-2 flex items-center gap-1 text-xs text-[#9a4a3d]"><Clock3 className="size-3" />Cancellation closed</p>}</div></div></article>; }) : <div className="rounded-[1.5rem] border border-dashed border-[#17362d]/20 bg-white p-10 text-center"><CalendarDays className="mx-auto size-7 text-[#a65f3d]" /><h2 className="mt-4 font-display text-3xl">No bookings yet.</h2><p className="mt-2 text-sm text-[#65756e]">Your confirmed sessions will appear here.</p><Button asChild className="mt-6 rounded-full bg-[#254d3f] text-white"><Link href="/schedule">Explore schedule</Link></Button></div>}</div>
    <p className="mt-5 text-xs leading-5 text-[#738078]">Demo rule: cancellation and full refund are available until two hours before the session start time. This state is stored only in your browser.</p></>;
}
