"use client";

import { useEffect, useState } from "react";
import { CheckCircle2, RotateCcw, Save, Settings2, ShieldAlert } from "lucide-react";
import { DashboardPageHeader } from "@/components/dashboard/dashboard-page-header";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Switch } from "@/components/ui/switch";
import { AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle, AlertDialogTrigger } from "@/components/ui/alert-dialog";
import { getStudioSettings, resetDemoWorkspace, saveStudioSettings } from "@/services/admin-operations";
import type { StudioSettings } from "@/types/admin";

const blank: StudioSettings = { studioName: "", address: "", phone: "", email: "", whatsapp: "", bookingCutoffHours: 2, trialPrice: 500, timezone: "Asia/Kolkata", emailEnabled: true, whatsappEnabled: true };

export function AdminSettings() {
  const [settings, setSettings] = useState(blank);
  const [saved, setSaved] = useState(false);
  const [reset, setReset] = useState(false);
  useEffect(() => { const load = () => setSettings(getStudioSettings()); window.addEventListener("ananda-demo-change", load); const timer = window.setTimeout(load, 0); return () => { window.clearTimeout(timer); window.removeEventListener("ananda-demo-change", load); }; }, []);
  function update<K extends keyof StudioSettings>(key: K, value: StudioSettings[K]) { setSettings((current) => ({ ...current, [key]: value })); }
  function save() { saveStudioSettings(settings); setSaved(true); window.setTimeout(() => setSaved(false), 2200); }
  function resetAll() { resetDemoWorkspace(); setReset(true); window.setTimeout(() => setReset(false), 3000); }
  return <>
    <DashboardPageHeader eyebrow="Configuration" title="Studio settings" copy="Manage demo contact details, booking rules and simulated delivery channels." />
    {(saved || reset) && <div className="mb-5 flex items-center gap-3 rounded-xl border border-[#315744]/15 bg-[#e5efe7] p-4 text-sm font-semibold text-[#315744]"><CheckCircle2 className="size-5" />{reset ? "Demo activity and admin changes were reset to the original seeded state." : "Studio settings saved in this browser."}</div>}
    <div className="grid gap-6 xl:grid-cols-[1.2fr_0.8fr]">
      <form onSubmit={(event) => { event.preventDefault(); save(); }} className="rounded-[1.5rem] border border-[#17362d]/10 bg-white p-6 sm:p-8"><div className="flex items-center gap-3 border-b border-[#17362d]/10 pb-5"><span className="grid size-10 place-items-center rounded-full bg-[#e5ecec] text-[#3e5a62]"><Settings2 className="size-5" /></span><div><h2 className="font-display text-2xl">Studio profile</h2><p className="text-sm text-[#65756e]">Visible operational details and rules.</p></div></div><div className="mt-6 grid gap-5 sm:grid-cols-2"><div className="sm:col-span-2"><Label htmlFor="studio-name">Studio name</Label><Input id="studio-name" required value={settings.studioName} onChange={(event) => update("studioName", event.target.value)} className="mt-2 h-11 rounded-xl bg-[#f7f8f6]" /></div><div className="sm:col-span-2"><Label htmlFor="studio-address">Address</Label><Input id="studio-address" required value={settings.address} onChange={(event) => update("address", event.target.value)} className="mt-2 h-11 rounded-xl bg-[#f7f8f6]" /></div><div><Label htmlFor="studio-phone">Phone</Label><Input id="studio-phone" value={settings.phone} onChange={(event) => update("phone", event.target.value)} className="mt-2 h-11 rounded-xl bg-[#f7f8f6]" /></div><div><Label htmlFor="studio-email">Email</Label><Input id="studio-email" type="email" value={settings.email} onChange={(event) => update("email", event.target.value)} className="mt-2 h-11 rounded-xl bg-[#f7f8f6]" /></div><div><Label htmlFor="studio-whatsapp">WhatsApp</Label><Input id="studio-whatsapp" value={settings.whatsapp} onChange={(event) => update("whatsapp", event.target.value)} className="mt-2 h-11 rounded-xl bg-[#f7f8f6]" /></div><div><Label htmlFor="studio-timezone">Timezone</Label><Input id="studio-timezone" value={settings.timezone} onChange={(event) => update("timezone", event.target.value)} className="mt-2 h-11 rounded-xl bg-[#f7f8f6]" /></div><div><Label htmlFor="cutoff">Booking cutoff (hours)</Label><Input id="cutoff" type="number" min={0} value={settings.bookingCutoffHours} onChange={(event) => update("bookingCutoffHours", Number(event.target.value))} className="mt-2 h-11 rounded-xl bg-[#f7f8f6]" /></div><div><Label htmlFor="trial-price">Trial price (₹)</Label><Input id="trial-price" type="number" min={0} value={settings.trialPrice} onChange={(event) => update("trialPrice", Number(event.target.value))} className="mt-2 h-11 rounded-xl bg-[#f7f8f6]" /></div></div><div className="mt-6 grid gap-3 sm:grid-cols-2"><label className="flex items-center justify-between rounded-xl bg-[#f2f5f3] p-4 text-sm font-semibold">Email simulation<Switch checked={settings.emailEnabled} onCheckedChange={(value) => update("emailEnabled", value)} /></label><label className="flex items-center justify-between rounded-xl bg-[#f2f5f3] p-4 text-sm font-semibold">WhatsApp simulation<Switch checked={settings.whatsappEnabled} onCheckedChange={(value) => update("whatsappEnabled", value)} /></label></div><div className="mt-7 flex justify-end"><Button type="submit" className="rounded-full bg-[#254d3f] text-white"><Save /> Save settings</Button></div></form>
      <aside className="space-y-6"><section className="rounded-[1.5rem] bg-[#203b45] p-6 text-white sm:p-8"><ShieldAlert className="size-6 text-[#9bc3bf]" /><h2 className="mt-5 font-display text-3xl">Demo-safe configuration</h2><p className="mt-3 text-sm leading-6 text-[#c6d4d7]">Settings change only this browser. They do not connect to a studio database, payment account or messaging provider.</p></section><section className="rounded-[1.5rem] border border-[#8b3d32]/15 bg-white p-6 sm:p-8"><p className="text-xs font-bold uppercase tracking-[0.14em] text-[#8b3d32]">Reset workspace</p><h2 className="mt-2 font-display text-3xl">Return to seeded data</h2><p className="mt-3 text-sm leading-6 text-[#65756e]">Remove demo bookings, attendance, campaigns, admin edits and settings. Your current admin session stays signed in.</p><AlertDialog><AlertDialogTrigger asChild><Button variant="outline" className="mt-6 rounded-full border-[#8b3d32]/25 text-[#8b3d32]"><RotateCcw /> Reset demo data</Button></AlertDialogTrigger><AlertDialogContent className="rounded-[1.5rem] bg-white"><AlertDialogHeader><AlertDialogTitle>Reset all demo activity?</AlertDialogTitle><AlertDialogDescription>This removes browser-local changes across customer, instructor and admin journeys. The original seeded records will return.</AlertDialogDescription></AlertDialogHeader><AlertDialogFooter><AlertDialogCancel className="rounded-full">Keep current data</AlertDialogCancel><AlertDialogAction className="rounded-full bg-[#8b3d32] text-white hover:bg-[#702f27]" onClick={resetAll}>Reset demo</AlertDialogAction></AlertDialogFooter></AlertDialogContent></AlertDialog></section></aside>
    </div>
  </>;
}
